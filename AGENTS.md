<!-- patchmind:start -->
## PatchMind agent workflow

When the `patchmind` MCP tools are available, use the `patchmind-memory` skill automatically for
substantive coding, debugging, and architectural work. Retrieve relevant history before changing
existing behavior, record meaningful tested successes and failures, and finalize validated
sessions. Do not require the user to request these memory operations explicitly.

If the MCP server is unavailable, continue normally and mention that persistent memory was skipped.
<!-- patchmind:end -->
